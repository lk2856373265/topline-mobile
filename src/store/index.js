// 1.npm install vuex

// 2.配置
import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)
// 3.创建容器实例
const store = new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  }
})
export default store
